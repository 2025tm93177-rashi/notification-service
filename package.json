{
  "name": "notification-service",
  "version": "1.0.0",
  "description": "Banking notification microservice for email and SMS alerts.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node scripts/seed.js",
    "reset-db": "node scripts/reset-db.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "docker": "^1.0.0"
  }
}
